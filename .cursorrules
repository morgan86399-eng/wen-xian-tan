# 問仙壇（Wen Xian Tan）專案開發規範與避坑指南

> **最後更新日期**：2026-09-06
> **適用範圍**：所有參與「問仙壇」前端、後端、樣式改版之 AI 助理（特別是 Gemini 模型專屬深度思考協議）與工程師。

---

## 🧠 【Gemini 模型專屬】深度推論與執行鐵則（Why - How - Deduce Protocol）
> **生效範圍**：嚴格生效於 **Gemini 模型（Google Antigravity / Gemini 3.8 / Gemini 系列代理）**。

1. **三維根因發想法（嚴禁表面治標）**：
   - 面對使用者提出的任何排版、功能或文案反饋，**嚴禁只做表面微調**（例如隨意塞個 `<br>` 或調整一兩個字）。
   - **每一步都必須發想並嚴密推論**：
     - **為什麼（Why）**：追溯底層架構、CSS 盒模型、Grid 比例（如左欄佔比是否大於分割線位置）、字級（font-size）與全形中文字元寬度的物理衝突。
     - **怎麼做（How）**：從最底層的容器架構、幾何邊界（如 `max-width: 44%` 小於 `48%` 分割線）與語義結構設計根本解法，徹底杜絕任何再發可能。
     - **推論依據（Deduction）**：每一步的尺寸、字數、換行斷點、字級都必須具備精確的像素與字元推論計算，並清楚向使用者呈現推論邏輯。

---

## ⚠️ 【最高優先級 UI 避坑鐵則】Hero 卡片文字與 48% 中線防切規範

### 1. 致命問題根因剖析（Architectural Root Cause）
1. **分割線與欄寬矛盾**：
   - `.app-hero--editorial::before` 畫了一條 **48%** 的垂直微光分割線。
   - 原先 `.app-hero--editorial` 的 Grid 是 `minmax(0, 1.12fr) minmax(260px, .88fr)`，左欄佔比達 **56%**，天生會穿過 48% 的中線！
   - 之前只限制 `.hero-description`，但 `.hero-copy` 和 `.hero-main-title` 允許成長到 620px，導致標題與內容直接被 48% 直線腰斬穿刺！
2. **標題未做語義階層化**：
   - 長標題（如 13 個全形字）在字級較大時，無法在單行容納，若無語義分行，瀏覽器會在第 9 個字處硬折，導致文字直接落在分割線上被切成兩半（如「決」字被切，「策局」掉落）。
3. **副標題字數過長產生孤字行（Widow Words）**：
   - 單行文字超過 24 字時，容器容納不下，導致最後 1~3 個字被擠到下一行（如「節奏。」孤零零一行），破壞整體視覺平衡。

### 2. 幾何級防禦規範（Geometric Defense Rules）
1. **CSS 幾何硬約束（容器級防禦，絕不可破）**：
   - `.hero-copy` 容器最大寬度在桌面端必須鎖定在 **`max-width: 44%`**（或 `max-width: 420px`），且具有 `padding-right: 24px`。
   - 數學鐵則：`44% < 48%`。容器右界永遠距離中線至少 4%（約 48px）+ 24px padding = **72px 以上安全帶**！文字物理上絕對不可能碰觸到分割線！
2. **主標題階層化語義排版（Brand & Subtitle Layering）**：
   - 長標題必須採用語義雙層結構或明確斷行：
     - 第一行（品牌）：`問仙壇`
     - 第二行（定位）：`智能人生決策局`
   - 標題字級配合容器限制在合理範圍（如 `clamp(2.1rem, 3.6vw, 2.9rem)` 與副標 `clamp(1.35rem, 2.2vw, 1.85rem)`），字尾與中線永遠保持 100px 以上安全距離。
3. **副標題 18~22 字對稱排版鐵則（杜絕孤字行）**：
   - 每一行短句長度**嚴格控制在 18~22 個中文字**，每句結尾強制加上 `<br class="hero-break-desktop">`。
   - 嚴禁超過 24 字，嚴禁在行尾產生 1~3 字孤立單字（如「節奏。」）。
   - 示範黃金排版：
     - 第一句（21字）：`面臨感情去留、轉職合夥的人生十字路口？<br class="hero-break-desktop">`
     - 第二句（22字）：`讓冷靜客觀的 AI 大腦，精算走向與破局時機。<br class="hero-break-desktop">`
     - 第三句（20字）：`<span class="hero-keep-together">先看透天命利弊報告</span>，再做最清醒的決定！`
---

## 🎨 品牌視覺資產規範
1. **頂部導覽列 Logo**：
   - 預設採用「純陽 1：乾元九陽天日（☰）」（`assets/logo_chunyang_sun.svg`）。
   - 其他純陽款式（七星劍、混元金葫）與仙道款式均完整備份於 `assets/` 目錄中，並可透過 `logo_preview.html` 進行即時切換。
2. **黑金神壇色調**：
   - 統一遵循黑金漸層（`--gold-gradient`）、深紫宵暗底（`#0C0A1C`）與純陽真火光暈（`#F59E0B`、`#DC2626`）。

---

## 🚀 部署與測試流程
1. **構建驗證**：任何改動必須通過 `npm run build`（Vite 打包與靜態資源拷貝）。
2. **單元與整合測試**：執行 `npm test`（必須 100% 通過 57 項後端與安全測試）。
3. **Cloudflare Pages 部署**：
   ```bash
   npx wrangler pages deploy dist --commit-dirty=true
   git push origin main
   ```
