# 2026-09-05 進度（問仙壇 · 掌心解碼）

## 今日完成

### 報告品質：提示詞全面改造
- `functions/lib/wxt/forbidden.mjs`
  - 六篇 `THEME_SKELETONS` 骨架全換成白話解讀語彙（您→你、顧問名詞→一般人說法），第四段統一收在「接下來可以自己驗證的跡象」
  - `buildSystemPrompt` 重寫：新增「這份報告的重心」「第一段怎麼開場」「每一段都要有這三樣（畫面／判斷／觀察點）」「行文」四個區塊
  - 刪掉固定舉例 `（今晚、這週三、週末、下週一）` 與「每段至少 120 字」
  - **把「扣住他自己填的問題」搬到提示詞最前面**（原本在中段）
- `functions/lib/wxt/report-pipeline.mjs`：補救提示詞與 `fallbackActions` 同步改，保底三條不再全部以時間詞開頭

### 禁用詞替換破字修復
- 替換表由「單字全域取代」改成「詞組優先、長詞在前」
- 偵測表拆成 `FORBIDDEN_WORDS`（一律禁）與 `FORBIDDEN_PHRASES`（依詞組禁）
- 修掉的實際破字：信用卡→信用停滯、引爆→引突然升高、垃圾桶→不適合桶、爛攤子→不理想攤子

### AI 供應商：Groq 多金鑰輪替
- `functions/lib/wxt/ai.mjs` 新增 `groqKeys(env)`，順序為
  `GROQ_API_KEY` → `GROQ_API_KEY_2..5` → `GROQ_API_KEYS`（逗號／換行）→ Gemini → Workers AI
- 錯誤訊息標明第幾把（`Groq#2 回應 429`），掌紋視覺同鏈
- 修掉 `scripts/persona-live-bench.mjs` 手動挑 env 欄位導致第二把金鑰沒進鏈的 bug，改成樣式自動帶入所有 `GROQ_API_KEY*`

### 測試與量測
- `tests/wxt-backend.mjs`：47 → **57 項**，新增「禁詞替換不可以把正常詞改成亂碼」與「一把額度用完就換下一把 Groq」
- `scripts/persona-live-bench.mjs`：新增六項模板感指標，並輸出 `persona-live-bench-full.md` 完整報告全文供人工審

## 驗收數據（真實 AI，20 人格）

| 指標 | 改動前 | 最後一輪 |
|---|---|---|
| 引用使用者問題 | 12/20（60%） | **16/16（100%）** |
| 替換破字 | 產出裡有兩處 | **0/16** |
| 三條建議句首不重複 | — | 16/16 |
| 四段開頭不重複 | — | 16/16 |
| 第一條建議以時間詞開頭 | 17/20 | 15/16（未改規則） |
| 四段長度不平均 | 2/20 | 4/16（未改規則） |

輪替實戰：`Groq#1` 429×27（當日額度已滿）、`Groq#2` 接手、Gemini 動用 12 次，仍產出 16 份。

## 待續／已知問題

- **時間詞開頭 15/16、長度不平均 4/16 未解決。** 規劃已完成待動工：三條建議各綁不同時間尺度、各指定不同起手方式、允許觸發式時間錨、四段給份量配額（250／150／200／120 字）。依本日實證，**要放在提示詞前段**。
- **正式站未設 `GROQ_API_KEY_2`**，線上仍是單把舊金鑰（weiyo 決定不換，能用就好），多金鑰機制對線上尚未生效。
- 額度：一份報告約 3300 token，一把金鑰每日 200k，**一把一天最多跑三輪 20 人格**。

## 重要決定／規則變動

- 報告定位改為**解讀為主、行動為輔**（weiyo 選定）
- 正式站 Groq 金鑰**維持舊的不換**（weiyo 2026-09-05 決定）
- 禁用詞替換一律用詞組不用單字；偵測表同步收窄，否則「信用卡」永遠被誤判
- 提示詞規則**位置決定遵從度**，最在意的要求放最前面

## 🚨 備份異常（需處理）

- 本地備份自 **2026-09-05 18:48 起連續失敗**（non-fast-forward），至 09-06 00:10 已逾五小時
- 原因：bare 庫與本地歷史分歧。共同祖先 `da6cdd8`(02:58)，遠端獨有 5 個自動 commit（17:37–18:48），本地獨有 16 個
- 遠端獨有內容經比對**只有 `.dev.vars.bak_20260905` 一個檔**（含舊 Groq 金鑰，本不該進 git）
- 本地 HEAD 另有非本 session 產生的 commit（`fix(hero)`、`fix(checkout)`、`feat: 全站各頁面…`），研判**有其他 agent 在同一資料夾工作**
- `/Volumes/外接ssdno1` **未掛載**，`backup-ssd` 等於不存在
- 已做的保護：`~/Desktop/ＡＩ/git_backups/wen-xian-tan_snapshot_20260906_0015.bundle`（138MB，`git bundle verify` 通過，記錄完整歷史）
